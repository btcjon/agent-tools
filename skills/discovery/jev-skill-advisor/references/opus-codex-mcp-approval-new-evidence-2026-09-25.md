Objective: Resolve a newly observed Codex CLI blocker for the already-approved Jev capability bridge.

Deliverables: Concise recommendation, material risks, exact acceptance checks. Advise only: no edits, mutations, commits, pushes, supervision or polling.

Acceptance: A fresh `codex exec --ephemeral` in this host can call `skill_suggest` and receive `notion.cli.page_read` without a human approval prompt or changing the global approval policy; exact five tool allowlist, server-side receipt checks, and other MCP configurations remain intact. Preserve intentional approval for any future tools. State what this proves versus ChatGPT Desktop.

Constraints: User authorized the cross-harness rollout and no-prompt Jev routing. Do not disable global security approvals or broaden to all MCP servers. Applicable instructions: `/Users/jonbennett/.agents/AGENTS.md`, `/Users/jonbennett/Library/CloudStorage/Dropbox/Projects/!launch/AGENTS.md`. Local registration code: `/Users/jonbennett/Library/CloudStorage/Dropbox/Projects/agent-tools/skills/discovery/jev-skill-advisor/adapters/registration/bridge_registration.py`; config `~/.codex/config.toml` jev-capability-bridge table.

Evidence: `codex mcp get jev-capability-bridge` shows enabled stdio with exact five enabled_tools and the host-local command. Direct registered-path canary passes five schemas, live Notion read parity, and forged-receipt denial. Fresh `codex exec --ephemeral -s workspace-write --json` exposed the tool and attempted `skill_suggest`, but error was `MCP tool call requires approval, but approval policy is never`; no Jev result. Official current documentation https://learn.chatgpt.com/docs/extend/mcp lists `default_tools_approval_mode` and per-tool `tools.<tool>.approval_mode`, values `auto`, `prompt`, `writes`, `approve`. Proposed: set server default `prompt`, exact currently allowed tools to `approve` only if read/telemetry semantics justify it; add installer tests preserving unrelated config, verify fresh CLI call. Alternative: only preapprove `skill_suggest`/`skill_read` and keep `notion-fetch` prompted, but this might break the no-interruption Notion read path under global never.

Questions: (1) Which exact five tool approval modes should we use for this bounded server? (2) Should server default remain `prompt` to fail closed for future tools? (3) What live test is sufficient to accept the fix?
