# Host-local Jev bridge bundle: Mac cutover boundary

The Mac bridge's release/profile data is under ~/.local/state/jev-skill-advisor/releases. Fresh Codex, Cursor and Pi launches now resolve `runtime/current` to a reviewed host-local bundle. A preexisting Cursor MCP process was still on the old Dropbox venv at final inspection; refresh that one process before calling all live Mac sessions cut over. Do not equate registered-path canaries with Codex Desktop in-thread discovery proof.

The builder is adapters/registration/freeze_bundle.py. It exports only skills/discovery/jev-skill-advisor from pinned Git revision 3fc6118, exports the archived uv.lock's MCP dependency hashes, builds a wheel offline, installs it non-editably into a host-local venv, copies run_bridge.py, replaces the MCP entrypoint with an isolated Python launcher, rejects .pth, editable installs, secrets, and Dropbox path bytes, then writes a file-hash manifest and removes write permission. Existing bundle builds verify and return unchanged.

The active guarded bundle is /Users/jonbennett/.local/state/jev-skill-advisor/runtime/bundles-guarded/a528d001220e-dc3bac9. `verify` checks 1,126 entries and an exact repeat returned unchanged. Its physical launcher and manifest pin the external interpreter and libpython, and its launch guard checks those hashes plus the active release before adding `--expected-release`. A wrong-release launch fails before server startup. The release pin remains a528d001220eec155f1172f3b9b76793177ae2479dd5d054ae246c047e2e26bc. The Python suite including registration/builder/pointer/guard tests passed 431/431; Pi Node suite passed 13/13. The prior reviewed bundle `bundles-reviewed/a528d001220e-3fc6118` remains as rollback. A Grok 4.7 review-repair worker timed out without a handoff; the lead inspected and repaired its partial edits and verified the result.

Build or verify from the agent-tools repo:

```sh
skills/discovery/jev-skill-advisor/.venv/bin/python skills/discovery/jev-skill-advisor/adapters/registration/freeze_bundle.py build --repo /Users/jonbennett/Library/CloudStorage/Dropbox/Projects/agent-tools --output-root /Users/jonbennett/.local/state/jev-skill-advisor/runtime/bundles-guarded --revision dc3bac9 --release a528d001220eec155f1172f3b9b76793177ae2479dd5d054ae246c047e2e26bc
skills/discovery/jev-skill-advisor/.venv/bin/python skills/discovery/jev-skill-advisor/adapters/registration/freeze_bundle.py verify --bundle /Users/jonbennett/.local/state/jev-skill-advisor/runtime/bundles-guarded/a528d001220e-dc3bac9
```

Cutover evidence: installed Codex/Cursor registrations and Pi helper point through one stable `runtime/current` pointer. The Mac preflight backups are under `~/.local/state/jev-skill-advisor/registration-backups/host-local-cutover-20260925/`; eight baseline file hashes and the 958-symlink inventory stayed unchanged immediately after the first pointer flip. A subsequent intentional Codex-only approval-policy edit is backed up separately there; no other server or top-level setting changed. The registered Codex/Cursor canaries and installed Pi helper returned exactly five tools with schema SHA256 `164f8b94bfc71efc375e25caebabefd1bab7cbf45400c8073f8131e4a11cc8b2`, and the live 5,691-byte Notion read SHA256 `2597f9841c9fc25d13775d5957f84852a69d7bfea3e192a8a9033b18a513021b` matched direct `ntn`; forged receipts denied. Codex/Cursor import provenance was host-local. A fresh Pi CLI turn selected `notion.cli.page_read`. Rollback through the prior reviewed bundle passed a canary and the pointer was restored to the guarded bundle. A fresh server's `lsof` showed only host-local or uv executable/library mappings; its `cwd` inherited this Dropbox checkout, which Opus accepted as distinct from a code import. The old Cursor MCP PID must still be refreshed once the Mac UI is unlocked. See Opus review `/Users/jonbennett/.codex/consult-routing/results/27941df347864808b0eb00155a4b780c.txt`.

Codex CLI acceptance: a fresh CLI turn initially reached `skill_suggest` but was blocked because the tool required approval while the host policy was `never`. The installer now keeps this server's default at `prompt` and explicitly approves only its five pinned read/receipt-checked tools. `codex mcp get` still lists five. Fresh ephemeral Codex CLI calls to `skill_suggest` and `notion-fetch` completed with no approval event; the registered-path forged-receipt canary still denies. Repeating the installer is unchanged. This does not prove an already-running ChatGPT Desktop task has refreshed its MCP catalog.

Keep native Notion routes and skill symlinks until their independent gates pass; the remote host and in-app session adoption are separate evidence boundaries.
