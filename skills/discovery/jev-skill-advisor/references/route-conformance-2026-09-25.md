# Shared routing policy and Pi read canary — 2026-09-25

Decision: retain Hermes Jev bridge as default and Pi's CLI-backed Notion route. Do not claim MCP parity on Pi or promote the Hermes native hint. Astra recommended testing the common policy and each adapter's actual contract, not adding a new Pi MCP route without a measured need.

`tests/test_notion_host_transport.py` now exercises three policy outcomes: preferred MCP available, MCP unavailable with declared CLI fallback, and neither usable. It also exercises Hermes `prepare_notion_runtime` with a synthetic ready CLI and an unavailable CLI, ensuring it does not open MCP after declaring the fallback. `adapters/pi/bridge-protocol.test.ts` asserts the Pi registration selects `cli`, binds the release, and does not silently request an MCP fallback. These are routing-policy and adapter-contract checks, not transport equivalence.

Fresh local checks: `.venv/bin/python -m pytest -q` passed 486; `node --test adapters/pi/*.test.ts` passed 14. A read-only installed Pi bridge canary against the active Mac snapshot passed: five pinned tools, `notion.cli.page_read` selected, 5,691 markdown bytes matching an independent `ntn` GET at SHA-256 `2597f9841c9fc25d13775d5957f84852a69d7bfea3e192a8a9033b18a513021b`, and a forged receipt denied. It reported schema SHA-256 `164f8b94bfc71efc375e25caebabefd1bab7cbf45400c8073f8131e4a11cc8b2`. The canary printed no page body or ID. Earlier installed Pi model and progressive-disclosure checks are recorded in `adapters/pi/INTEGRATION.md`; this new canary directly exercised the bridge, not a fresh model turn.

The Hermes bridge/native 20-session comparison remains descriptive performance evidence, not write prevention. The repaired evaluator audits direct and deferred calls after execution, validates route/model/discovery, and rejects unknown tools. After Astra's review, its remote metadata projection also rejects malformed or unsupported deferred-call arguments and missing target names instead of silently dropping them; fixtures cover JSON strings, objects, malformed JSON, and a name-only wrapper. Three historical heading-count runs used a terminal call; those rows must not be cited as proof of zero side effects. No runtime pointer, gateway config, symlink, or production transport was changed here.
