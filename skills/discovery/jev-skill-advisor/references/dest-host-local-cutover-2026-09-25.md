# Dest Hermes host-local Jev bridge, 2026-09-25

`dest` is verified as `vmi3528421` (Linux x86_64). Its active release remains `31cd802e18e22d60ced684ddff378d6a8141a72d0dc0212012bb5c96776230b2`, implementation revision `7df33b87e8db04e26967018824bf585ea4dc6bef`. The first worker timed out without a handoff after building an unused bundle incorrectly bound to Mac release `a528…`; that bundle remains as evidence and was never activated. The lead separately proved revision `3fc6118` against dest's active release: the source diff from `7df…` added the host CLI route and changed the MCP server, but an offline live Hermes/Notion canary passed. The guarded revision `dc3bac9` changes no `src/jev_skill_advisor` files relative to `3fc6118`.

The live stable pointer `/home/dev/.local/state/jev-skill-advisor/runtime/current` now resolves to immutable bundle `bundles-guarded/31cd802e18e2-dc3bac9`. Independent verification found 1,128 files, a pinned `/usr/bin/python3.12` hash, and a mutation-free `build` repeat. Its launcher validates Python/libpython and the active release on every start, then pins `--expected-release`; a wrong-release launch exited with `expected_release_mismatch`. Package and Python imports resolve under host-local/OS paths, not Dropbox.

Read-only host inspection on 2026-09-25 found no `libpython` dynamic dependency for `/usr/bin/python3.12`: `readelf -d` lists `libm`, `libz`, `libexpat`, and `libc` as its needed shared libraries. The dest bundle therefore pins the interpreter binary but has no separate `libpython_path`/hash to check. A copied-bundle launcher test in `test_runtime_guard.py` now proves an interpreter-hash mismatch fails before the MCP server starts. This does not claim that all OS shared libraries are pinned.

The only Hermes config edit changed the `jev-skill-advisor` command from `/home/dev/.hermes/hermes-agent/venv/bin/skill-advisor-mcp` to `runtime/current/bin/skill-advisor-mcp`. The byte-preserving CAS installer checked the original config hash `91bbb80d…` and produced `686683f5…`; the reverse dry-run reproduces the original hash. Backup: `/home/dev/.local/state/jev-skill-advisor/registration-backups/dest-cutover-20260925/config-before.yaml` (mode 0600, original hash). The warehouse plugin files and active release pointer retained their original hashes. `hermes mcp list` still has both native `notion` and the five-tool `jev-skill-advisor`; `hermes mcp test` launched the new pointer and found exactly five tools.

Read-only canary through the new pointer: `notion.mcp.fetch`, five schemas SHA256 `164f8b94bfc71efc375e25caebabefd1bab7cbf45400c8073f8131e4a11cc8b2`; a known page's 19,046-character body SHA256 `809ec2f35427611dabdb5e62b1856d72138f678932382abbca2bf65bb787d499` matched a separate call through Hermes's existing Notion OAuth MCP; forged receipt denied as `unauthorized_capability`. A fresh Hermes one-shot agent session returned only `readable`, and the capability event log correlated a selected `notion.mcp.fetch` with a successful `bridge_read`. No page content or credentials were printed by the canary.

Rollback drill: CAS restored the old Hermes command, both the old five-tool server and native Notion MCP connected, CAS removed the new pointer, and the old server still connected. The pointer and new command were then restored; fresh discovery and live read/denial parity passed again. The old executable and config backup remain available. A long-lived Hermes child PID 194427, started before the switch, still has the former command; do not claim that process migrated until its owner is safely refreshed. No Notion write, release promotion, skill-symlink retirement, or other Hermes config edit occurred.
