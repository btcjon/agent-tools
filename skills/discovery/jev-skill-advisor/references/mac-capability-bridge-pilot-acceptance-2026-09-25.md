# Mac Jev / Notion capability bridge pilot — 2026-09-25

Pilot release `a528d001220eec155f1172f3b9b76793177ae2479dd5d054ae246c047e2e26bc` is active on this Mac, bound to implementation revision `3fc6118`. The old release is retained for compare-and-swap rollback. The remote Hermes release pointer hash was the same before and after Mac activation; Hermes code and configuration were not changed.

The live Notion skill census contained 628 unique pages. One new package explicitly aliased an old active primary ID; the local stage omitted the superseded page, retained its alias, and verified a 627-package immutable snapshot. Nothing was edited or deleted in Notion. The snapshot has 616 selectable skills and 11 explicitly disabled skills. Three representative `skill-search` tasks selected the same skill before and after activation, while reporting the new snapshot ID afterward.

The Mac release offers one executable Notion capability card, `notion.cli.page_read`, not the 45 MCP operations available on dest Hermes. It uses the absolute tested `ntn 0.23.2`, a declared host-local credential and workspace, a full release pin, an exact receipt, a static schema pin, and a read-only argv allowlist. The 20-case fresh Jev holdout was 10/10 page reads selected and 10/10 unrelated, search, or write requests abstained. The final test suite passed 417 tests.

Cursor and Codex CLI registrations each preserve unrelated MCP configuration and are mutation-free on repeat. Cursor reports the bridge ready and lists exactly five tools. A fresh Cursor agent invoked `skill_suggest` and received `notion.cli.page_read` with auto-review enabled. In noninteractive ask mode without auto-review, Cursor's tool approval prompt rejected the call before the bridge; this is a harness operating condition, not a bridge authorization. Both exact registered stdio commands were tested with a restricted GUI-like environment: Jev selected the CLI read card and the fetched page markdown matched an independent Notion read byte-for-byte. A deliberately bad token failed despite a saved CLI login. Write and unrelated requests produced no capability and an attempted read was denied `unauthorized_capability`.

The current Codex desktop task's tool inventory predates registration, so this task cannot prove an in-app MCP refresh. Codex CLI sees the enabled five-tool server, and its exact registered command passed the end-to-end canary. A fresh desktop task or app refresh still needs confirmation. Pi remains unregistered because its MCP adapter is not wired. Organic route preference over time, OAuth refresh on dest, and rollback execution are production gates, not pilot claims. Native Notion tools remain available; no symlinks were retired. Graphify is not in live routing and should only be tested offline against held-out misses.
