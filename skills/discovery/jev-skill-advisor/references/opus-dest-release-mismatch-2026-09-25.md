Objective: Resolve a newly discovered release mismatch before any dest (`vmi3528421`) host-local Jev runtime switch.

Deliverables: Concise recommendation, material risks, exact acceptance changes. Plan/critique only: no edits, mutations, commits, pushes, worker supervision or polling.

Acceptance: Dest's existing Hermes Jev skill and Notion MCP routes remain live; any new runtime is host-local, bound to the actually active immutable release and verified source; five schemas, a real Notion read parity check, forged-receipt denial, rollback, mutation-free repeat. No blind release promotion, credentials copy, native-route removal, or claim that Mac and dest have the same auth identity.

Constraints: User authorized cross-harness rollout, but prior gate forbids unverified production promotion and symlink retirement. Preserve unrelated remote state. No `ntn` on dest PATH; existing Hermes Notion OAuth MCP is live. `/Users/jonbennett/.agents/AGENTS.md` and `/Users/jonbennett/Library/CloudStorage/Dropbox/Projects/!launch/AGENTS.md` apply. Local repo `/Users/jonbennett/Library/CloudStorage/Dropbox/Projects/agent-tools`.

Evidence: SSH BatchMode verified `dest` hostname `vmi3528421`, Linux x86_64; `/home/dev/.local/bin/uv` exists. Grok 4.7 worker timed out at 900 seconds without handoff. It left an immutable Linux bundle `/home/dev/.local/state/jev-skill-advisor/runtime/bundles/a528d001220e-3fc6118`; independent `freeze_bundle.py verify` passed 1,127 files, source revision `3fc6118`, manifest release `a528d001220eec155f1172f3b9b76793177ae2479dd5d054ae246c047e2e26bc`, external `/usr/bin/python3.12` pinned by SHA256. No `runtime/current` pointer was created. Dest's `releases/current-release.json` is `31cd802e18e22d60ced684ddff378d6a8141a72d0dc0212012bb5c96776230b2`; its release manifest is delivery with codex/generic/hermes profiles, but there is no `a528...` release directory on dest. `hermes mcp list` still shows `notion` and `jev-skill-advisor` enabled. Mac's a528 bridge is live for fresh CLI/Pi/Cursor paths; Codex CLI approval fix tested. Existing Mac Cursor GUI process remains legacy until user unlocks Mac.

Questions: (1) Should dest get a fresh bundle bound to its active `31cd...` release, or should we prepare a separate properly gated release promotion? (2) Which exact artifacts prove that revision `3fc6118` is compatible with dest's current release and transport? (3) What is the smallest safe remote live canary given Notion MCP OAuth and no `ntn`?
